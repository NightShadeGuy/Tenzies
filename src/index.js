import React from "react"
import ReactDOM from "react-dom"
import "./index.css"
import App from "./App"

const root = document.getElementById("root");
const display = ReactDOM.createRoot(root);

display.render(<App />);

